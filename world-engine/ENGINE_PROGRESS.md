# Engine Development Progress

当前主线为 MUD 世界模拟、确定性和持久化可靠性。不新增 UI、登录或部署功能。

## 当前阶段

PR #61 在数据库启动恢复基础上修复 Issue #62 暴露的基础回归，并展开此前被
失败链条挡住的扩展测试。当前分支仍须通过最新 head 的远端完整验收后才能合并。

| 能力 | 当前实现与验收 |
|---|---|
| 世界级 ID | 修复同前缀跨命名空间碰撞；旧存档高水位迁移、续接和溢出检查已覆盖。 |
| 自然、生态与城市压力 | 修复物种适宜度、维护能力、零值、部分状态与坏资源处理。 |
| 治理、过程、冲突与组织 | 修复重复过程、动员作用范围、招募预算分配和过程插入上限。 |
| 世界一致性 | 修复零种群索引、非有限数值、删除后的重复修复以及检测计数。 |
| 确定性源码审计 | 核心目录空豁免基线通过；随机决策使用命名随机流，真实时间隔离在运行环境层。 |
| 完整测试入口 | 根目录与 world-engine 统一自动发现全部回归；本地 83 个脚本全部通过。 |
| JSONL 存档与恢复 | API/自动保存/查询已存在；新增启动恢复、结构校验、写入序号选择和真实进程重启专项。 |
| SQLite / PostgreSQL | 尚无实际驱动与 SQL 事务；配置占位不等于数据库完成。 |
| 默认管线收敛 | 信息、文化和信仰 helper 已存在；正式默认入口与更深长期演化仍需继续。 |
| 文明、科技、基础设施 | 已有基础模块；时代演化、扩散和依赖网络待深化。 |
| 生产存储保障 | 并发写保护、事务化提交、容量管理与生产备份恢复仍待完成。 |

## 验收口径

本地环境 Node 22.16.0。83 个回归脚本通过不替代远端 Node 20/22、World Engine
Tests 和独立 1000 tick 压测。历史绿色图标曾掩盖失败，两个 workflow 已使用
显式 bash/pipefail；本次不删除失败断言、不提高过程数量上限、不添加审计豁免。

旧 75%、88%、96%、100% 等估算没有统一分母，不能作为生产验收进度。当前以
已实现范围、实际测试结果和剩余边界记录进度。

详细变更见 ENGINE_HARDENING.md；原始失败证据见 CI_VALIDATION_STATUS.md。
后续先完成本分支真实验收，再推进关系数据库、事务化存档与并发写保护。
