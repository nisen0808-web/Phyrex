# Engine Development Progress

当前主线为 MUD 世界模拟、确定性和持久化可靠性。不新增 UI、登录或部署功能。

## 已验收主线

PR #61 已合并到 main，合并提交 5e84da7d8fe9caff2638c8900c0436beb75b7a43。
验收 head b6b413747a429aee86bcd0537bc505b8f987048e 的 Node 20、Node 22、
根目录和 world-engine 目录均为 83/83 通过，100 tick 和独立 1000 tick 通过。
原始日志已下载核验，来源为 World Engine CI 34397910221 和 World Engine Tests
34397910259。这一结果取代 CI_VALIDATION_STATUS.md 最后一段“远端待验收”，
其历史失败证据仍然保留。核心源码纯度基线仍为空，没有增加跳过项或放宽断言。

## 当前开发层

真实 PostgreSQL 事务存档，不再仅保留 provider 配置占位。实现异步存储工厂、
固定版本驱动和依赖锁、显式版本迁移、快照/版本/事件原子提交、预期版本冲突保护、
请求幂等、内容校验和、连接池与超时、参数化查询、导入导出 CLI。

本地 Node 22.16.0 完整回归 84/84 通过。新增 6 组无数据库契约测试单独标识；
真实 SQL 验收由 World Engine PostgreSQL 在临时 PostgreSQL 18、Node 20/22 上
执行 14 组事务/回滚/独立进程竞争/重连/CLI 测试。该层尚待最新 head 远端验收，
本地契约通过不代替 PostgreSQL 实测通过。

| 能力 | 当前实现与边界 |
|---|---|
| 世界模拟与确定性 | #61 回归修复及完整测试已验收；更深环境循环、代际、文明和科技演化仍需深化。 |
| 世界 ID 与源码审计 | 同前缀跨命名空间碰撞已修复；命名随机流与运行环境时间边界已有测试。 |
| JSONL | 既有接口、自动保存、查询与启动恢复继续兼容；仍是同步单写原型。 |
| PostgreSQL 存储 | 已实现真实异步驱动、版本迁移和事务存档；等待本层远端完整验收。 |
| PostgreSQL API/runtime | 尚未切换现有同步 API 和 runtime loop；异步等待、背压和 SQL 恢复属于下一层。 |
| 运维与生产保障 | 尚未配置生产数据库；保留策略、生产备份恢复、权限开通、复制容灾和部署未验收。 |

下一步先完成 PostgreSQL 本层真实验收，再接入异步 runtime 和 API，不继续增加管理页面。
旧百分比没有统一验收分母，不再用作生产完成度。存储接口和验收边界见 POSTGRES_DATABASE.md。
