# Engine Development Progress

当前主线为 MUD 世界模拟、确定性和持久化可靠性。不新增 UI、登录或部署功能。

## 已验收主线

PR #61 已合并，提交 5e84da7d。验收 head b6b4137 的 Node 20/22、根目录与
引擎目录回归为 83/83，100 tick 与独立 1000 tick 通过。此前 CI 假绿证据保留在
CI_VALIDATION_STATUS.md；工作流使用显式 bash/pipefail，不放宽断言。

PR #63 已合并，提交 373f8bab。验收 head 4bf429e 的完整回归 84/84，真实
PostgreSQL 18 专项在 Node 20/22 各 14/14，100/1000 tick 通过。实际日志已核对。
来源：World Engine CI 34401860948、Tests 34401860990、PostgreSQL 34401860815。

## 当前开发层

PostgreSQL 持久化世界运行器：从明确指定的已存档世界恢复；隔离副本推进确定性
模拟，事务确认以后才发布新世界。保留单个候选批次与相同请求 ID 重试，不重跑
模拟或重复消耗随机流；版本冲突停写；有限自动重试、背压、停机排空与信号处理。

本地 Node 22.16.0 完整回归 86/86。新增 13 组可控存储生命周期契约，不能代替
真实 SQL 验收。PostgreSQL workflow 保留原有 14 组存储测试，并新增 11 组运行器
集成测试，覆盖独立进程竞争、重启、事务故障与停止。远端验收以本 PR 最终 head
的日志为准；没有用本地测试宣称生产可用。

| 能力 | 当前实现与边界 |
|---|---|
| 世界模拟、ID 与源码审计 | #61 修复和完整回归已验收；更深自然、代际、文明科技演化仍待深化。 |
| JSONL | 同步接口与重启恢复继续兼容，仍是单写原型。 |
| PostgreSQL 存储 | #63 真实驱动、迁移、原子存档、并发冲突、幂等与校验已验收。 |
| PostgreSQL 世界运行器 | 本层增加提交后发布、重试、持续运行、停机和独立 CLI；正在进行远端 SQL 验收。 |
| 现有 HTTP 与玩家操作 | 未切换至新运行器；后续要统一命令入口与提交后响应，不能直接共享可变世界。 |
| 生产运行 | 未配置生产数据库，未验收备份恢复、保留策略、主从复制、领导者租约与部署。 |

下一节点是异步 HTTP/外部命令接入持久化运行器，不继续增加管理页面。
不使用没有统一验收分母的百分比。实现与完整边界见 POSTGRES_DURABLE_RUNTIME.md。

本层真实 SQL 首轮测试发现 JSONB 键顺序导致重启漂移，已通过逐 tick 规范化修复，
数组顺序保留。新增 5 组本地排序/完整状态测试和 36 实体真实 SQL 重启场景。
另修复事件系统向关系衰减传配置对象时产生 NaN 的接口缺陷，未放宽存档校验。
